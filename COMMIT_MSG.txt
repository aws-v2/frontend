feat(s3): implement bucket creation with validation and API integration

## Changes Made

### Bucket Creation Feature
- Add comprehensive bucket name validation following AWS S3 naming rules
  - Length validation (3-63 characters)
  - Character validation (lowercase, numbers, periods, hyphens only)
  - Format validation (no consecutive periods, no IP address format)
  - Start/end with letter or number requirement
- Implement complete form data collection for bucket creation
  - General configuration (name, region, bucket type)
  - Object ownership settings
  - Block public access configuration
  - Versioning settings
  - Tags support
  - Encryption configuration (SSE-S3, SSE-KMS, DSSE-KMS)
  - Object lock settings
- Add API integration ready for backend
  - Endpoint: POST /api/v1/buckets/create-bucket
  - Proper error handling with try-catch
  - Toast notifications for user feedback
  - Console logging for debugging
  - API call commented out pending backend implementation
- Import apiClient for future API integration

### Technical Details
- Created `validateBucketName()` helper function with AWS S3 compliance
- Enhanced `handleCreateBucket()` to be async and collect all form fields
- Structured bucket data object matching backend API expectations
- Maintained backward compatibility with local store fallback
- Added proper TypeScript error typing

### Files Modified
- src/modules/s3/pages/CreateBucketPage.vue (+67, -9 lines)

### Notes
- API integration is ready but commented out (lines 84-88)
- Uncomment API call when backend endpoint is available
- All validation and data formatting is production-ready
- Console logs bucket configuration for debugging

Co-authored-by: AI Assistant <assistant@example.com>
